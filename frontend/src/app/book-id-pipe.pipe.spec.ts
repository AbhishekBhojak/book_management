import { BookIdPipePipe } from './book-id-pipe.pipe';

describe('BookIdPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BookIdPipePipe();
    expect(pipe).toBeTruthy();
  });
});
